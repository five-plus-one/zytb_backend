# ====================================
# 生产环境配置
# ====================================
# 文件名: .env.production
# 用途: 生产服务器部署使用
# ⚠️ 警告: 此文件包含敏感信息，不应提交到Git仓库

# 环境模式
NODE_ENV=production

# 服务器配置
PORT=11452

# 数据库配置 (生产环境MySQL)
# ⚠️ 请修改为生产环境的实际配置
DB_HOST=your-production-db-host
DB_PORT=3306
DB_USER=your-production-db-user
DB_PASSWORD=your-production-db-password-CHANGE-THIS
DB_NAME=volunteer_system

# JWT 配置
# ⚠️ 必须修改为强密钥！使用 openssl rand -base64 32 生成
JWT_SECRET=CHANGE-THIS-TO-A-STRONG-SECRET-KEY-IN-PRODUCTION
JWT_EXPIRE=7d

# LLM API 配置 (生产环境)
# ⚠️ 请使用生产环境的API密钥
LLM_API_KEY=your-production-llm-api-key
LLM_BASE_URL=https://api.ai.five-plus-one.com/v1
LLM_MODEL=deepseek-chat

# OpenAI Embedding 配置
OPENAI_API_KEY=your-production-openai-api-key
OPENAI_API_URL=https://api.ai.five-plus-one.com/v1/embeddings
EMBEDDING_MODEL=text-embedding-3-small

# 短信服务配置 (生产环境必须配置)
SMS_ACCESS_KEY=your-sms-access-key
SMS_SECRET_KEY=your-sms-secret-key
SMS_SIGN_NAME=志愿填报系统
SMS_TEMPLATE_CODE=your-template-code

# 文件上传配置
UPLOAD_PATH=/var/app/uploads
MAX_FILE_SIZE=5242880

# 日志配置
LOG_LEVEL=info
LOG_FILE_PATH=/var/log/volunteer-system

# CORS 配置 (生产环境只允许特定域名)
# ⚠️ 请修改为实际的前端域名
CORS_ORIGIN=https://your-frontend-domain.com,https://www.your-domain.com

# Redis 配置 (生产环境推荐使用)
REDIS_HOST=your-redis-host
REDIS_PORT=6379
REDIS_PASSWORD=your-redis-password

# 调试模式 (生产环境关闭)
DEBUG=false
VERBOSE_LOGGING=false

# 性能监控 (可选)
# SENTRY_DSN=your-sentry-dsn
# APM_SERVER_URL=your-apm-server-url

# SSL/TLS 配置 (如果需要)
# SSL_KEY_PATH=/path/to/ssl/key.pem
# SSL_CERT_PATH=/path/to/ssl/cert.pem
